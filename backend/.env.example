# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://localhost:27017/himflax

# JWT
JWT_SECRET=
JWT_EXPIRE=7d

# CORS — comma-separated origins
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@himflax.com

# Upload
MAX_FILE_SIZE=5242880
